# Direct RUNTIME dependencies only (#206). Test/dev tooling lives in
# requirements-dev.in. The deployable lockfile is requirements.txt —
# regenerate it (never hand-edit) with:
#
#   pip install pip-tools
#   pip-compile --generate-hashes --output-file=requirements.txt requirements.in
#   pip-compile --generate-hashes --output-file=requirements-dev.txt requirements-dev.in
#
# Use Python 3.12 (the CI/container interpreter) when compiling.
fastapi>=0.141.1,<1.0
uvicorn[standard]>=0.52.1,<1.0
httpx>=0.27.0,<1.0
supabase>=2.0.0,<3.0
sqlalchemy>=2.0.36,<3.0
alembic>=1.14.0,<2.0
psycopg[binary]>=3.2.0,<4.0
PyJWT>=2.8.0,<3.0
cryptography>=48.0.1,<49.0.0
python-dotenv>=1.0,<2.0
sentry-sdk[fastapi]>=2.66.1,<3.0
apscheduler>=3.10.0,<4.0
redis>=5.2.0,<8.0
numpy>=2.0.0,<3.0
