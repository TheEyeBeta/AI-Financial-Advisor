[pytest]
testpaths = tests
# Baseline floor — set just below current actual branch coverage (~70%) so
# regressions fail CI. Ratchet --cov-fail-under up as new tests close the gap.
addopts = --cov=app --cov-branch --cov-fail-under=69 --cov-report=term-missing --cov-report=xml
