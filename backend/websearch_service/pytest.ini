[pytest]
testpaths = tests
# Baseline floor — set just below current actual coverage so any regression
# fails CI. Ratchet --cov-fail-under up as new tests close the gap. See
# docs/ci/CI_GUIDELINES.md.
addopts = --cov=app --cov-branch --cov-fail-under=74 --cov-report=term-missing --cov-report=xml
